[project]
name = "anonforge"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "aiomysql",
    "pydantic",
    "python-dotenv",
    "pillow",
    "httpx",
    "crewai",
]

[dependency-groups]
dev = []